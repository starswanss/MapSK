# ระบบ / เครื่องมือ
.DS_Store
Thumbs.db
node_modules/
*.log

# การตั้งค่าเครื่องมือเฉพาะเครื่อง (ไม่ต้องขึ้น GitHub)
.claude/

# หมายเหตุ: อย่าใส่ images/ ที่นี่ — ต้องให้รูป (โหมดในเครื่อง) ขึ้น GitHub ด้วย
